server {
    listen 80;

    location / {
        resolver 127.0.0.11 valid=5s;
        set $upstream app-blue:5000;
        proxy_pass http://$upstream;
        proxy_set_header Host $host;
        proxy_connect_timeout 1s;
    }
}
